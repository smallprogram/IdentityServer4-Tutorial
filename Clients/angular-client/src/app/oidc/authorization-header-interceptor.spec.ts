import { AuthorizationHeaderInterceptor } from './authorization-header-interceptor';

describe('AuthorizationHeaderInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthorizationHeaderInterceptor()).toBeTruthy();
  });
});
